commands:
  start: npm run start
  nodever: node --version
  npminfo: |
    echo "version: $(npm --version)"
    echo "configuration:"
    npm config list
  lsdir:
    args:
    - G_DIR # the directory to list
    script: ls $G_DIR
